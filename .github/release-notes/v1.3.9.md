## Changes

- Added a shared type-import strategy boundary for the native parser and Ghidra CParser.
- Added a restricted CParser header adapter with bounded input, strict UTF-8 validation and no preprocessor directives.
- Kept the native parser as the default importer with unchanged type and metadata results.
